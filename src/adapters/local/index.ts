/**
 * Local (Ollama) adapter barrel.
 *
 * 0.1.0 does NOT publish a deep import path — see
 * `_workspace/01_architecture.md` §8. Tree-shaking from the main barrel is
 * sufficient.
 */

export { LocalModelAdapter } from './LocalModelAdapter';
