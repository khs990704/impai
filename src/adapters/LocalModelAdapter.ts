/**
 * Re-export shim. Implementation moved to `./local/LocalModelAdapter.ts` per
 * the parent-agent layout (`src/adapters/local/`).
 *
 * Spec source: `_workspace/02_api_spec.md` §B.3.
 */

export { LocalModelAdapter } from './local/LocalModelAdapter';
