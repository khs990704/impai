/**
 * Re-export shim. Implementation moved to `./openai/OpenAIAdapter.ts` per
 * the parent-agent layout (`src/adapters/openai/`).
 *
 * Spec source: `_workspace/02_api_spec.md` §B.1, §B.5, §B.6.
 */

export { OpenAIAdapter } from './openai/OpenAIAdapter';
